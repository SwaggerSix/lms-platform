// Test stub for the `server-only` marker module. In the Next.js build this
// import is provided by the framework to fail client bundles; under vitest it
// is a no-op so server modules can be unit tested.
export {};
